// C program to understand difference between
// pointer to an integer and pointer to an
// array of integers.
#include <stdio.h>
#include <stdlib.h>

#define test(x, ...) x, ##__VA_ARGS__

int main()
{

    // Pointer to an array of 5 integers\
    jkklkdlkfd
    // int x = 2;
    //scanf("%d\n", &x);
    //char a[x];
    //scanf("%s", a);
    int x = 0, y = 0, z = 0;
    {
        printf("%d %d %d", test(x, y, z));
        printf("b");
    };
}
